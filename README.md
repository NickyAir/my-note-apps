## Upcoming Updates

Here's the list for the upcoming updates and issues

---

- **Issues :**

Users accidentally press the **delete** button, which removes their **important** notes.

Users are not redirected back to the edit field when pressing the **edit** button.

Users cannot save their notes while offline.

---

- **Updates :**

- [ ]  Improve the frontend and its logic. For example, when a user clicks the delete button, show a confirmation modal to ensure the user truly wants to delete the note, preventing accidental deletions.

- [ ] Improve the frontend and its logic. For example, when a user clicks the edit button, they should be automatically directed to the edit field without needing to scroll manually.

(Optional)

- [ ] Made this website PWA (Progressive Web Application).
- [ ] Improve the website's responsiveness and make it more interactive.
- [ ] Allow the website to store data in IndexedDB when offline, and then sync the stored data to the cloud database once the user is back online.

---

Don't include this `README.md` file to `main` branch! 
 Make sure to mark each checkbox ([x]) once you’ve applied the corresponding update.
